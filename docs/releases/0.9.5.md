新增各模型的推理强度选择，默认使用最低可用档位，协作翻译也可手动调整；改善模型切换、列表刷新和连接状态的稳定性。

Choose the reasoning level for each model, including collaborative translation, with the lowest supported level selected by default. Improved reliability when switching models, refreshing the model list, and checking connection status.
