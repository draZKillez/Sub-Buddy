修复协作翻译已有进度却不显示预计时间的问题；同次打开 App 内重试可复用最近一次提取或 OCR 的字幕，视频与轨道未变化时无需重新扫描。补翻会携带逐条校验原因，少量顽固条目在最后一轮单独处理，保留已完成译文并限制重试次数。

Fix missing ETAs during collaborative translation and reuse the most recently prepared subtitles for retries within the same app session when the video and track are unchanged. Targeted repairs now include per-cue validation reasons and isolate small stubborn tails in the final retry round while preserving accepted translations and bounded retries.
