修复 0.9.3 单条补翻时多行字幕触发 `invalid_json_schema` 的请求构造回归，改善错误提示并停止对此类无效请求重复重试；同时修复重复保存译文、取消后后台任务继续运行及输出安全检查等稳定性问题。

Fix the 0.9.3 request-schema regression affecting single-cue repairs of multiline subtitles, with clearer errors and no repeated retries of invalid requests; also improve translation checkpoint handling, cancellation, and output safety.

验证 / Validation：本地 181 项测试通过、7 项条件测试跳过，Intel 与 Apple Silicon Release 构建通过；真实字幕的 Codex 补翻尚未实测。181 local tests passed, 7 opt-in tests skipped, and both Intel and Apple Silicon release builds passed; live Codex subtitle-repair verification remains pending.
